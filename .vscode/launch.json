{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Commander",
            "cargo": {
                "args": ["build", "--bin=commander", "--package=commander"],
                "filter": {
                    "name": "commander",
                    "kind": "bin"
                }
            },
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
